////////////////////////////////////////////////////////////////
// @file main_create_n_vertex_graph.c
// @brief A sample application.
////////////////////////////////////////////////////////////////

#include "graph.h"

graph G;

////////////////////////////////////////////////////////////////
// Entry point for the application.
////////////////////////////////////////////////////////////////
int main(int argc, char* argv[]) {

  int n = strtol(argv[1], 0, 10);

  //Make sure that G is an empty graph.
  //memset(&G, 0, sizeof(graph));
  
  G.vertex_count = n;

  write_graph(&G);

  return 0;
}

